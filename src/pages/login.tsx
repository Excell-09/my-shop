import Layout from '@/components/Layout';

const Login = () => {
  return (
    <Layout>
      <section className='min-h-screen'>
        <div>
          <h2>Login</h2>
        </div>
      </section>
    </Layout>
  );
};

export default Login;
